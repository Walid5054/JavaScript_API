const hena = {
  name: "hena",
  age: 10,
  friends: ["hero alom", "bappa"],
};
const { name, age, friends } = hena;
console.log(name, age, friends);

// with array
const person = ["helo alom ", { name: "producer" }, "hena", "bappara"];
const [hero, heroine] = person;
console.log(hero, heroine);
